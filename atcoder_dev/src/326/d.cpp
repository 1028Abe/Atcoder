#include <bits/stdc++.h>

using namespace std;

int main(){

    int n;
    cin >> n;
    string  R, C;
    cin >> R >> C;

    vector<string> grid(n);
    for(int i = 0; i < n; i++){
        for(int j = 0; j < n; j++){
            
        }
    }

    return 0;
}